<UserControl x:Class="View.PersonList.PersonList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:personList="clr-namespace:View.PersonList"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance personList:DesignPersonListPresentation, IsDesignTimeCreatable=True}">
    <ListView ScrollViewer.VerticalScrollBarVisibility="Hidden" BorderThickness="1" ItemsSource="{Binding People}" >
        <ListView.View>
            <GridView>
                <GridViewColumn DisplayMemberBinding="{Binding FirstName}">
                    <GridViewColumnHeader>First Name</GridViewColumnHeader>
                 </GridViewColumn>
                <GridViewColumn DisplayMemberBinding="{Binding FamilyName}">
                    <GridViewColumnHeader>Last Name</GridViewColumnHeader>
                </GridViewColumn>
            </GridView>
        </ListView.View>
        <ListView.ItemContainerStyle>
            <Style TargetType="{x:Type ListViewItem}">
                <Setter Property="IsSelected" Value="{Binding IsSelected.Value}"/>
            </Style>
        </ListView.ItemContainerStyle>


    </ListView>
</UserControl>
